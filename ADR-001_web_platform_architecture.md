# ADR-001: สถาปัตยกรรมเว็บแพลตฟอร์มสำหรับระบบผังน้ำชุมชนกึ่งอัตโนมัติ

**สถานะ**: ยอมรับแล้ว (Accepted) — Ton ยืนยันรับทางเลือก B (Serverless: GitHub Pages + Supabase, ตัด FastAPI) — ดู ADR-002 สำหรับการตัดสินใจเพิ่มเติมเรื่องเอนจิน
**วันที่**: 30 ส.ค. 2569
**ผู้ตัดสินใจ**: Ton
**เอกสารอ้างอิงที่ Ton ส่งให้**: `system diagram.html` (ผัง Mermaid), `Architecture.html` (สเปค stack/workflow),
`Frontend สำหรับชาวบ้านปรับแก้ผังน้ำ.html` (ต้นแบบ frontend ที่ใช้งานได้จริงด้วย Cytoscape.js)

**คำสั่ง Ton (verbatim)**: "ต่อไปลองวิเคราะห์ความเป็นไปได้ของวิธีที่ระบุในไฟล์ที่ส่งให้ 1. ใช้รูปแบบเดียวกับ
โปรเจคที่เคยทำ (DBใช้supabase และ deployด้วยgithub page) 2. ฉันชอบฟังก์ชั่นfrontendที่ออกแบบนี้มาก ให้ใช้ระบบ
นี้เลยในการพัฒนาต่อ"

## บริบท (Context)

โครงการนี้ผ่านมาแล้ว 19 รอบของการสร้าง pipeline แบบ Python ออฟไลน์ (`04_scripts/*.py`) ที่ทำหน้าที่:
แปลง KML/shapefile → จัดลำดับโครงข่ายน้ำ (topology) → จัดวาง schematic แบบ dendrogram → วาดผังน้ำทางการด้วย
matplotlib — ทุกขั้นตอนผ่านการตรวจสอบยืนยันจาก Ton อย่างละเอียด (โครงสร้าง parent/child, ทิศทาง, ชื่อที่ถูกตัด
ทอน, artifact ที่ต้องกรองทิ้ง ฯลฯ) และมีบั๊ก/เคสขอบที่แก้ไปแล้วจำนวนมาก (บั๊กวัดขนาดตัวอักษร, บั๊ก field-width
truncation, บั๊ก polygon ความกว้างคลองปนกับแหล่งน้ำจริง เป็นต้น) — โค้ดชุดนี้ "ใช้งานได้จริงและผ่านการพิสูจน์แล้ว"
ไม่ควรทิ้งหรือเขียนใหม่โดยไม่จำเป็น

ตอนนี้ Ton ต้องการยกระดับจาก script ออฟไลน์ไปเป็นเว็บแพลตฟอร์มจริงที่ชุมชนเข้าถึงและแก้ไขได้ พร้อมส่งเอกสาร
ออกแบบมาให้ 3 ไฟล์ ซึ่งมีข้อขัดแย้งกันเองบางจุด:
- `system diagram.html` และ `Architecture.html` เสนอ stack: **React.js/React Flow (frontend) + FastAPI
  (backend) + GeoPandas/PySheds/NetworkX (engine) + PostgreSQL/PostGIS (DB) รันบน Cloud Server (EC2/Droplet)**
- `Frontend สำหรับชาวบ้านปรับแก้ผังน้ำ.html` เป็นต้นแบบที่ Ton ชอบและใช้งานได้จริงแล้ว แต่เขียนด้วย **HTML/JS
  ธรรมดา + Cytoscape.js** (ไม่ใช่ React เลย ไม่มีขั้นตอน build ใด ๆ)

ข้อจำกัดที่ Ton ระบุตรง ๆ:
1. ให้ใช้รูปแบบเดียวกับโปรเจคก่อนหน้า: **DB = Supabase, Deploy = GitHub Pages**
2. ให้ใช้ frontend ตามต้นแบบ Cytoscape.js นี้ต่อไปเลย ไม่เปลี่ยน

ข้อจำกัดเชิงเทคนิคที่สำคัญ: **GitHub Pages เป็น static hosting เท่านั้น รันเซิร์ฟเวอร์ฝั่งหลังบ้าน (เช่น FastAPI)
ไม่ได้เลย** — นี่คือจุดที่สเปคที่ Ton ส่งมา (Architecture.html) ขัดแย้งโดยตรงกับข้อจำกัดที่ 1 ที่ Ton ระบุเอง
จึงเป็นประเด็นหลักที่ ADR ฉบับนี้ต้องแก้

## การตัดสินใจ (Decision)

ใช้สถาปัตยกรรมแบบ **Serverless JAMstack**: Frontend แบบ static (ไฟล์ Cytoscape.js ต้นแบบเดิม บวก Supabase
JS client) โฮสต์บน GitHub Pages เรียก Supabase (Postgres + PostGIS extension + Auth + Realtime) โดยตรง
**ไม่มี FastAPI/Python backend server ที่รันตลอดเวลา** — ส่วน "เอนจินประมวลผลเชิงพื้นที่" (DEM, topology, จัดวาง
schematic) ยังคงเป็น Python pipeline ออฟไลน์ที่มีอยู่แล้ว (ไม่ต้องเขียนใหม่) รันโดยผู้ดูแลระบบ (หรือรันอัตโนมัติ
ผ่าน GitHub Actions เป็นครั้งคราว) แล้ว push/upsert ผลลัพธ์ (JSON ของกราฟโครงข่าย) เข้า Supabase แทนการประมวลผล
สดทุกครั้งที่มีคนเปิดเว็บ

## ทางเลือกที่พิจารณา (Options Considered)

### ทางเลือก A: ตามสเปคเดิมทุกประการ — React + FastAPI + PostgreSQL/PostGIS (self-hosted) + React Flow

| มิติ | การประเมิน |
|---|---|
| ความซับซ้อน | สูง — ต้องสร้าง/โฮสต์/ดูแล 3 ระบบแยกกัน (frontend build, API server, DB server) |
| ค่าใช้จ่าย | ต้องมี cloud server ที่รันตลอดเวลา (EC2/Droplet) — มีค่าใช้จ่ายรายเดือนต่อเนื่อง บวกค่า DB |
| ความสามารถขยาย | ดีถ้าต้องการสเกลใหญ่ในอนาคต — แต่เกินความจำเป็นของโปรเจคระดับ pilot 1 ตำบลไปมาก |
| ความคุ้นเคยของทีม | ต้องเรียนรู้ React Flow ใหม่ทั้งหมด (ยังไม่เคยสร้าง) และทิ้งต้นแบบ Cytoscape.js ที่ใช้งานได้แล้ว |
| ตรงกับข้อจำกัดที่ Ton ระบุ | **ขัดแย้งตรง ๆ** — Ton ขอ GitHub Pages ซึ่งรัน FastAPI ไม่ได้ |

**ข้อดี**: แยกส่วนชัดเจนตามหลักวิศวกรรมซอฟต์แวร์ทั่วไป, ควบคุม API เองได้เต็มที่, รองรับการขยายสเกลใหญ่
**ข้อเสีย**: ขัดกับสิ่งที่ Ton ขอโดยตรง, ต้องดูแลเซิร์ฟเวอร์ต่อเนื่อง (patch/uptime/ค่าใช้จ่าย), ทิ้ง frontend ที่
Ton ชอบ, ใช้แรงพัฒนามากที่สุดสำหรับโปรเจคที่ยังอยู่ในขั้น pilot

### ทางเลือก B (แนะนำ): Cytoscape.js แบบ static (GitHub Pages) + Supabase (Postgres+PostGIS, Auth, Realtime) + เอนจิน Python ออฟไลน์เดิม (ไม่เปลี่ยน)

| มิติ | การประเมิน |
|---|---|
| ความซับซ้อน | ต่ำ-กลาง — มี 2 ส่วนหลัก (static site + managed DB) เอนจินเดิมใช้ต่อได้เลย |
| ค่าใช้จ่าย | ใช้ free tier ได้จริงในสเกล pilot (GitHub Pages ฟรี, Supabase free tier: DB 500MB, เปิด PostGIS ได้) |
| ความสามารถขยาย | รองรับหลายสิบตำบล/การใช้งานระดับชุมชนได้สบาย ๆ ก่อนจะต้องอัปเกรดเป็น Supabase paid tier |
| ความคุ้นเคยของทีม | ตรงกับรูปแบบโปรเจคก่อนหน้าของ Ton เป๊ะ, ใช้ต้นแบบ Cytoscape.js เดิมได้โดยแทบไม่ต้องแก้, ใช้โค้ด Python เอนจินที่ผ่านการพิสูจน์แล้ว 19 รอบได้ต่อเนื่อง |
| ตรงกับข้อจำกัดที่ Ton ระบุ | **ตรงทั้ง 2 ข้อ** |

**ข้อดี**: ตรงกับสิ่งที่ Ton ขอทั้ง 2 ข้อ, โค้ดใหม่ที่ต้องเขียนน้อย (ส่วนใหญ่เป็นงาน "เชื่อมต่อ" ไม่ใช่สร้างใหม่),
ไม่ต้องดูแลเซิร์ฟเวอร์เอง, Supabase มี PostGIS extension ให้ใช้, Supabase Auth/RLS ใช้คุมสิทธิ์แก้ไขของชุมชนต่อ
ตำบลได้, Supabase Realtime เป็นของแถมฟรีถ้าอยากให้แก้ไขพร้อมกันหลายคนในอนาคต

**ข้อเสีย**: ไม่มีการประมวลผลเชิงพื้นที่แบบสดต่อคำขอ (DEM/PySheds/NetworkX ต้องรันแบบออฟไลน์ ไม่ใช่กดปุ่มแล้ว
คำนวณทันทีจากเบราว์เซอร์) — ต้องตัดสินใจว่าจำเป็นแค่ไหนที่ต้องประมวลผลใหม่แบบเรียลไทม์; Supabase free tier จะ
พักโปรเจกต์อัตโนมัติถ้าไม่มีการใช้งานนาน ๆ และมี cap พื้นที่เก็บข้อมูลถ้าโปรเจกต์โตเกิน pilot; การเรียก Supabase
ตรงจาก frontend (static site) ต้องระวังเรื่อง Row Level Security เพราะ anon key จะเปิดเผยอยู่ใน static site

### ทางเลือก C: Supabase DB + GitHub Pages frontend + เอนจินเป็น Supabase Edge Functions (Deno)

| มิติ | การประเมิน |
|---|---|
| ความซับซ้อน | กลาง — รวมทุกอย่างในแพลตฟอร์มเดียว (Supabase) แต่ต้องเขียน GeoPandas/PySheds/NetworkX ใหม่เป็น TypeScript/Deno |
| ค่าใช้จ่าย | ยังอยู่ใน free tier ได้ |
| ความสามารถขยาย | ดี |
| ความคุ้นเคยของทีม | ต้องเขียนโค้ดเรขาคณิตที่ผ่านการพิสูจน์แล้ว (robust_tangent, canonicalize_mainstem, dendrogram layout, การอ่าน shapefile) ใหม่ทั้งหมดในภาษา/รันไทม์อื่น — ความเสี่ยงสูง งานใหญ่ |
| ตรงกับข้อจำกัดที่ Ton ระบุ | ตรงในทางเทคนิค แต่แลกด้วยการเขียนโค้ดที่ใช้งานได้แล้วใหม่ทั้งหมด |

**ข้อดี**: ทุกอย่างอยู่แพลตฟอร์มเดียว ไม่ต้องมีขั้นตอนออฟไลน์แยก, รู้สึก "สด" กว่า
**ข้อเสีย**: Deno/edge functions ไม่มีไลบรารีเทียบเท่า GeoPandas/Shapely/PySheds — ต้องเขียนตรรกะเรขาคณิตที่ใช้
เวลาแก้บั๊กมาหลายเดือนใหม่จากศูนย์ ความเสี่ยงเกิด regression สูง ไม่คุ้มสำหรับโปรเจคขั้น pilot

## การวิเคราะห์ข้อแลกเปลี่ยน (Trade-off Analysis)

จุดแยกทางที่แท้จริงคือ **เอนจินเชิงพื้นที่จำเป็นต้องรันสดต่อคำขอผู้ใช้ หรือรันเป็นงานแบบ batch/ออฟไลน์ก็พอ** —
ดูจากวิธีที่โครงการนี้ทำงานมาตลอด 19 รอบ: ทุกเวอร์ชันของผังน้ำเกิดจากการรันสคริปต์ ตรวจสอบผลลัพธ์ แล้วค่อยส่งให้
Ton/ชุมชนดู — ไม่เคยมีความจำเป็นต้องประมวลผลฝั่งเซิร์ฟเวอร์แบบ on-demand เลย งาน "เอนจิน" (อ่าน shapefile/KML,
คำนวณ topology, จัดวาง dendrogram) เกิดขึ้นครั้งเดียวต่อการอัปเดตข้อมูล ไม่ใช่ครั้งเดียวต่อการเปิดหน้าเว็บ นั่น
หมายความว่าสมมติฐานของทางเลือก B (เอนจินรันแบบออฟไลน์) ไม่ใช่ข้อจำกัดที่ยอมแลกเพราะเงื่อนไขโฮสติ้ง แต่ตรงกับ
วิธีทำงานจริงของโปรเจคอยู่แล้ว ทางเลือก A (FastAPI สด) จะแก้ปัญหาที่โปรเจคนี้ไม่มีจริง ด้วยต้นทุนโครงสร้างพื้นฐาน
ที่จับต้องได้ ส่วนทางเลือก C ต้องเขียนโค้ดเรขาคณิตที่กว่าจะแก้บั๊กจนใช้งานได้ (robust_side_of, robust_tangent,
canonicalize_mainstem, บั๊กวัดขนาดตัวอักษรและการแก้ scale-consistency จากรอบ 18, บั๊ก truncation/artifact จาก
รอบ 19) ใหม่ในระบบนิเวศที่ไม่มีไลบรารีเทียบเท่า — ไม่คุ้มกับสิ่งที่ได้ (ไม่ต้องมีขั้นตอนแยก)

ทางเลือก B มีงานใหม่ที่ต้องทำจริงอยู่หนึ่งอย่าง: เปลี่ยนการรันสคริปต์ครั้งเดียว ๆ ให้กลายเป็นขั้นตอน "publish เข้า
Supabase" ที่ชัดเจน (จะรันมือ: รันสคริปต์ → upsert JSON เข้าตาราง Supabase, หรือรันอัตโนมัติผ่าน GitHub Actions
ตามตารางเวลา/webhook ก็ได้) — เป็นงานเพิ่มเล็กน้อยบนโครงสร้างที่มีอยู่แล้ว (สคริปต์สร้าง topology table และ
output ที่ตั้งชื่อเวอร์ชันอยู่แล้ว)

## ผลที่ตามมา (Consequences)

**สิ่งที่จะง่ายขึ้น**: การ deploy (แค่ git push ขึ้น GitHub Pages), การเข้าถึงของชุมชน (เปิดเบราว์เซอร์ได้ทันที
ไม่ต้องติดตั้งอะไร), การแก้ไขพร้อมกันหลายคน (Supabase Realtime), การควบคุมสิทธิ์ (Supabase Auth + RLS แทนการ
สร้างระบบ auth เอง), การใช้โค้ด Python เอนจินที่ผ่านการพิสูจน์แล้ว 19 รอบต่อได้เลยโดยไม่ต้องเขียนใหม่

**สิ่งที่จะยากขึ้น**: อะไรก็ตามที่ต้องการการประมวลผลฝั่งเซิร์ฟเวอร์แบบสดทันทีที่ผู้ใช้คลิก (ซึ่งจากที่สังเกตมา
ยังไม่เคยเป็นความจำเป็นจริง); การควบคุมคุณภาพข้อมูลที่ชุมชนแก้ไข ต้องฝากไว้ที่ RLS policy หรือ validation
เบา ๆ ฝั่ง frontend แทนที่จะมี backend service ที่ตรวจสอบเข้มข้น

**สิ่งที่ต้องกลับมาทบทวนภายหลัง**:
- ความถี่ที่เอนจินออฟไลน์ต้องรันใหม่ (รันมือ vs. รันอัตโนมัติผ่าน GitHub Actions) เมื่อมีตำบลเพิ่มขึ้น
- ต้องการมุมมอง "แผนที่ภูมิศาสตร์อ้างอิง" คู่กับผัง schematic ตามที่ `system diagram.html` เสนอไว้หรือไม่ —
  เพราะ schematic ของเราตั้งใจไม่ใช้พิกัดจริง (ระยะห่างแบบ dendrogram ไม่ใช่สเกลระยะทางจริง ตามที่ยืนยันมาตลอด
  19 รอบ) ในขณะที่ขั้นตอน "Reverse Geocoding" ใน `Architecture.html` Phase 5 (แปลงตำแหน่งที่แก้บนผังกลับเป็น
  พิกัดภูมิศาสตร์จริง) สมมติว่าผังใช้พิกัดจริงอยู่แล้ว ซึ่งขัดกับแนวทาง schematic-only ที่ใช้มาตลอด — ต้องตัดสินใจ
  ชัดเจน: **คงเป็น schematic ล้วน (แบบที่ผ่านการตรวจสอบแล้ว)** หรือ **เพิ่มโหมดแผนที่จริงคู่กัน** (ขอบเขตงานใหญ่
  ขึ้นมาก)

**Frontend**: แนะนำให้คงต้นแบบ Cytoscape.js ตามที่ Ton เลือกไว้ ตัดแนวทาง React/React Flow ที่อีก 2 เอกสารเสนอ
ออกไป (เอกสารทั้งสองขัดแย้งกันเองอยู่แล้ว และ Ton เลือก Cytoscape.js ไปแล้ว) — ผลพลอยได้คือลดความซับซ้อนของการ
deploy บน GitHub Pages ลงอีก (ไม่ต้องมีขั้นตอน build เลย)

## รายการงานถัดไป (Action Items)

1. [ ] ตัดสินใจเรื่อง scope: คงผัง schematic ล้วน (ไม่มีพิกัดจริง) ตามที่ตรวจสอบมาแล้ว — หรือเพิ่มมุมมองแผนที่จริง
   คู่กัน (ขอบเขตงานใหญ่ขึ้น ต้องกลับไปดู Phase 5 ของ `Architecture.html` เรื่อง reverse geocoding)
2. [ ] สร้างโปรเจกต์ Supabase, เปิด PostGIS extension, ออกแบบ schema (tambon, waterway_line/node, water_source,
   hydraulic_structure, edit_version/audit) โดยอิงจากโครงสร้าง topology_table ที่มีอยู่แล้ว
3. [ ] กำหนดนโยบาย Row Level Security สำหรับสิทธิ์แก้ไขของชุมชน (รหัสผ่านต่อตำบลแบบเปิด vs. บัญชี Supabase Auth)
   และตัดสินใจ flow การตรวจสอบ/อนุมัติการแก้ไขจากชุมชน
4. [ ] ขยาย Python pipeline ที่มีอยู่ให้ส่งออกเป็น JSON รูปแบบที่ Supabase ใช้ได้ (nodes/edges/positions จากการ
   จัดวาง dendrogram) และเขียนสคริปต์ "publish" เล็ก ๆ (เริ่มจากรันมือก่อน ค่อยทำเป็น GitHub Action ทีหลังถ้าคุ้ม)
5. [ ] เชื่อมต้นแบบ Cytoscape.js เดิมเข้ากับ Supabase JS client: โหลดกราฟตอนเปิดหน้า, เปลี่ยน `exportData()`
   จาก console.log ให้เป็นการ upsert เข้า Supabase จริง
6. [ ] ตั้งค่า deploy บน GitHub Pages (repo static ล้วน ไม่ต้องมีขั้นตอน build)
7. [ ] ตัดสินใจขั้นตอน export ภาพผังทางการ: export แบบ client-side (canvas/SVG) เทียบกับคงสคริปต์ matplotlib
   เดิมไว้เป็นขั้นตอน "render ฉบับทางการ" (ยังรันแบบออฟไลน์ได้ ตามจังหวะที่ Ton ตรวจสอบ)
