# ADR-003: แหล่งข้อมูลเส้นทางน้ำของเอนจินใหม่ — OSM เป็นหลัก + เติม mitrearth (provincial GIS) อัตโนมัติสำหรับส่วนที่ขาด

**สถานะ**: ยอมรับแล้ว (Accepted)
**วันที่**: 30 ส.ค. 2569
**ผู้ตัดสินใจ**: Ton
**เกี่ยวข้องกับ**: ADR-002 (เขียนเอนจินใหม่จากศูนย์)

## บริบท (Context)

ใน Phase 1 (Data Ingestion) เวอร์ชันแรก ผู้พัฒนา (Claude) ใช้ shapefile ระดับจังหวัด (mitrearth: Major River /
minor stream / water body จาก `ARCHIVE_pilot_v1_offline_pipeline/01_data_raw/provincial_gis/`) เป็นแหล่งข้อมูล
เส้นทางน้ำหลักของเอนจินใหม่ — Ton ทักท้วงว่าเอกสารออกแบบ (`00_design_docs/Architecture.html`) ระบุไว้ชัดเจนว่า
**"ข้อมูลเส้นน้ำตั้งต้น: ฐานข้อมูล HydroRIVERS หรือ OpenStreetMap (OSM)"** ไม่ใช่ข้อมูล mitrearth ที่เป็นแหล่ง
เดียวกับที่ pipeline 19 รอบเดิมใช้ — การนำ mitrearth มาใช้เป็นฐานหลักของเอนจิน "ใหม่" จึงขัดกับเจตนารมณ์ของ
ADR-002 (ไม่นำ "วิธีเดิม" มาปนกับของใหม่) แม้ตัวโค้ดจะเขียนใหม่ทั้งหมดก็ตาม — Ton ยืนยันว่ายอมรับได้ถ้าจะใช้
mitrearth เป็นเพียง**ข้อมูลตรวจสอบเสริม**สำหรับเส้นทางน้ำที่ขาดหายไปจากข้อมูลที่แกะออกมา

## การสำรวจ (Investigation)

ทดสอบดึงข้อมูล OSM จริงสำหรับพื้นที่ตำบลนครป่าหมาก (ผ่าน Overpass API) พบข้อจำกัดและข้อค้นพบสำคัญ 2 เรื่อง:

1. **ข้อจำกัดเครือข่ายของสภาพแวดล้อมการพัฒนา**: ทั้ง cloud sandbox (ที่ Claude รันคำสั่งทั่วไป) และเครื่อง
   Linux VM ของ Ton ที่ mount `D:\AI\Water_diagram` ไว้ (ที่ใช้รันสคริปต์เอนจินทั้งหมดจนถึงตอนนี้) ถูกจำกัดด้วย
   allowlist เครือข่ายระดับองค์กร **เรียก Overpass API / HydroSHEDS โดยตรงไม่ได้เลย** (บล็อกด้วย 403
   blocked-by-allowlist) — แต่ browser Chrome จริงของ Ton (ผ่าน extension) มีอินเทอร์เน็ตเต็มรูปแบบ ทดสอบยิง
   คำขอ Overpass API ผ่าน `fetch()` จากหน้าเว็บสำเร็จ (POST ใช้ได้ แต่ GET ผ่านการนำทาง URL ตรง ๆ ติด HTTP 406
   จากการเจรจา content-type ของเบราว์เซอร์ — เป็นรายละเอียดปลีกย่อยของฝั่งเซิร์ฟเวอร์ Overpass ไม่ใช่ปัญหาเครือข่าย)
   → **ผลคือ: การเรียก OSM ต้องแยกเป็น "ขั้นตอนดึงข้อมูล" ต่างหากจาก "ขั้นตอนประมวลผลเอนจิน"** เพราะเอนจิน
   (สคริปต์ที่รันบน device_bash) ไม่มีเครือข่ายเรียก OSM ได้เอง สอดคล้องกับสถาปัตยกรรมแบบ batch/offline ที่
   ADR-001 วางไว้อยู่แล้ว (ในระบบจริง ขั้นตอนดึง OSM ควรรันผ่าน GitHub Actions หรือเครื่องที่มีอินเทอร์เน็ต แล้ว
   commit ไฟล์ผลลัพธ์เป็น raw data ให้เอนจิน offline อ่านต่อ ไม่ใช่ให้เอนจิน live-fetch เอง)
2. **ความครอบคลุมของข้อมูล OSM ในพื้นที่ชนบทน้อยกว่า mitrearth มาก**: ดึงข้อมูล waterway ทั้งหมดในกรอบพื้นที่
   ตำบลนครป่าหมาก (+buffer 300 ม.) จาก OSM ได้ **57 เส้น** (river 17, canal 26, stream 2, drain 11) เทียบกับ
   mitrearth ที่มี **129 เส้น** (major river 10 + minor stream 119) — ต่างกันมากกว่า 2 เท่า โดยเฉพาะคลอง/
   ลำธารสาขาย่อยระดับชุมชนที่ OSM มีการสำรวจ/แผนที่ชุมชน (community mapping) ยังไม่ครอบคลุมทั่วถึงในพื้นที่ชนบท
   แบบนี้ — ยืนยันด้วยตัวเลขจริง ไม่ใช่การคาดเดา

## การตัดสินใจ (Decision)

ใช้กลยุทธ์ **"OSM หลัก + เติม mitrearth อัตโนมัติ"** (ตัวเลือกที่ Ton เลือกจาก 4 ทางเลือกที่นำเสนอ):

- **OSM (ผ่าน Overpass API) เป็นแหล่งข้อมูลเส้นทางน้ำตั้งต้นหลัก** ของเอนจินใหม่ ตรงตามเจตนารมณ์ของ
  `Architecture.html` และ ADR-002 (ข้อมูลมาตรฐานสากล ใช้ได้ทั่วประเทศ ไม่ผูกกับข้อมูลชุดเดียวกับ pipeline เดิม)
- **ข้อมูล mitrearth (provincial GIS) ที่ไม่ทับซ้อนกับเส้นทาง OSM ในพื้นที่เดียวกัน จะถูกเติมเข้าไปในผลลัพธ์
  โดยอัตโนมัติ** (ไม่ใช่แค่ทำเป็นชั้นข้อเสนอแนะให้เลือกเองทีละเส้น) — แต่ทุกเส้นที่มาจาก mitrearth (ไม่ใช่ OSM)
  จะถูกติด property/flag `source: "mitrearth_supplement"` แยกจาก `source: "osm"` อย่างชัดเจนในข้อมูลกราฟ
  เพื่อให้ตรวจสอบย้อนกลับได้ และให้ชุมชนยืนยันความถูกต้องผ่าน Interactive Canvas ใน Phase 4 ได้ในภายหลัง
- เกณฑ์ "ทับซ้อนหรือไม่": เส้น mitrearth เส้นหนึ่งถือว่า "มีอยู่แล้วใน OSM" ถ้าอยู่ใกล้ (ภายในระยะที่กำหนด) กับ
  เส้น OSM เส้นใดเส้นหนึ่งตลอดความยาวส่วนใหญ่ของมัน มิฉะนั้นถือว่าเป็นเส้นที่ขาดหาย ต้องเติม (รายละเอียดพารามิเตอร์
  ระยะห่างจะปรับตามผลทดสอบจริงกับตำบลนำร่อง)

## สถาปัตยกรรมที่ปรับ (Consequences)

- แยกโมดูลเป็น 2 ส่วนชัดเจน:
  1. **"Fetch" (ต้องใช้อินเทอร์เน็ต)**: สคริปต์ดึงข้อมูล OSM waterway ผ่าน Overpass API สำหรับตำบลที่กำหนด
     บันทึกเป็นไฟล์ raw (เช่น JSON/GeoJSON) — **ไม่รันผ่าน device_bash** (ไม่มีเครือข่ายเรียกได้) ระหว่างพัฒนา
     รันผ่าน browser ของ Ton (Claude in Chrome) แทน ในระบบจริงควรย้ายไปรันบน GitHub Actions หรือเครื่องที่มี
     อินเทอร์เน็ต แล้ว commit ไฟล์ raw เข้าโปรเจกต์
  2. **"Process" (ไม่ต้องใช้อินเทอร์เน็ต)**: เอนจินหลัก (`01_data_ingestion.py` และไฟล์ถัดไป) อ่านไฟล์ raw ที่ดึง
     มาแล้ว (ทั้ง OSM และ mitrearth) จากดิสก์ ทำ spatial merge/supplement ตามกฎข้างต้น — ยังคงเป็น offline
     batch ตาม ADR-001 เหมือนเดิมทุกประการ
- ทุกเส้นทางน้ำในผลลัพธ์กราฟมี property บอกแหล่งที่มา (`osm` / `mitrearth_supplement`) เพื่อความโปร่งใสและ
  ตรวจสอบย้อนกลับได้ — สอดคล้องกับวินัยของโปรเจกต์เรื่อง "ห้ามกุข้อมูล ต้องระบุที่มาชัดเจนเสมอ"
- ต้องปรับ `01_data_ingestion.py` เดิม (ที่ใช้ mitrearth เป็นหลัก) ให้เป็นสถาปัตยกรรมนี้แทน — เขียนโมดูลใหม่:
  `fetch_osm_waterways.py` (ต้องอินเทอร์เน็ต, รันนอก device_bash) และ `merge_waterway_sources.py` (ผสาน
  OSM + mitrearth ตามกฎ supplement) ต่อจาก `01_data_ingestion.py` เดิม (ปรับให้โหลด mitrearth เป็นแค่แหล่ง
  เสริมแทนแหล่งหลัก)

## รายการงานถัดไป (Action Items)

1. [ ] ดึงข้อมูล OSM waterway เต็มรูปแบบสำหรับนครป่าหมาก บันทึกเป็นไฟล์ raw ในโปรเจกต์
2. [ ] เขียนโมดูลแปลง OSM Overpass JSON → GeoJSON เส้นทางน้ำ (พร้อม tag ประเภท: river/canal/stream/drain ฯลฯ)
3. [ ] เขียน logic ผสาน OSM + mitrearth (ตรวจทับซ้อนเชิงพื้นที่ เติมเฉพาะส่วนที่ขาด พร้อม flag แหล่งที่มา)
4. [ ] ปรับ `01_data_ingestion.py` ให้ใช้สถาปัตยกรรมนี้ ยังคงทั่วไป (generic) ไม่ผูกกับตำบลนำร่อง
5. [ ] สร้าง sanity check เปรียบเทียบผลลัพธ์ใหม่ (OSM+supplement) กับผังน้ำอ้างอิงจริงของนครป่าหมาก
