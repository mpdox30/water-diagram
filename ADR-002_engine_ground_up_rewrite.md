# ADR-002: เขียนเอนจินคำนวณ topology/schematic ใหม่ทั้งหมด แทนการนำ pipeline 19 รอบมาต่อยอด

**สถานะ**: ยอมรับแล้ว (Accepted)
**วันที่**: 30 ส.ค. 2569
**ผู้ตัดสินใจ**: Ton
**เกี่ยวข้องกับ**: ADR-001 (สถาปัตยกรรมเว็บแพลตฟอร์ม — Serverless: GitHub Pages + Supabase)

## บริบท (Context)

ADR-001 (ยอมรับแล้ว) กำหนดให้ใช้สถาปัตยกรรมแบบ serverless (GitHub Pages + Supabase) และตอนแรกแนะนำให้เอนจิน
เชิงพื้นที่คงเป็น Python pipeline เดิมที่มีอยู่ (จาก `ARCHIVE_pilot_v1_offline_pipeline/04_scripts/`) รันแบบ
ออฟไลน์ต่อไป — Ton ตอบกลับชัดเจนว่า **"วิธีนี้เป็นวิธีที่เราจะทดลองใหม่ ไม่เอาวิธีที่เคยทำมา 19 รอบมาปน"**

เมื่อถามยืนยันขอบเขต (เพราะตีความได้หลายแบบ) Ton เลือกตัวเลือก **"เขียนเอนจินใหม่ทั้งหมดจากศูนย์"** อย่างชัดเจน
— ไม่ใช่แค่แยกโฟลเดอร์/เอกสาร แต่หมายถึงไม่นำโค้ด/อัลกอริทึมจาก pipeline เดิมมาใช้เลย

จากนั้น Ton สั่งเพิ่มเติมว่า "งานที่เราเคยทำมา จัดใส่โฟลเดอร์แยกไว้จากกระบวนการที่กำลังจะทำนี้ เพื่อให้กลับไปทำ
ต่องานเดิมได้เมื่อต้องการ" — ดำเนินการแล้ว โดยย้ายงานเดิมทั้งหมดไปไว้ที่ `ARCHIVE_pilot_v1_offline_pipeline/`
(ดูรายละเอียดใน README.md อัปเดต 30 ส.ค. 2569)

**เหตุผลที่สมเหตุสมผลของการตัดสินใจนี้** (บันทึกไว้เพื่อความเข้าใจในอนาคต แม้ Ton ไม่ได้อธิบายเหตุผลไว้ตรง ๆ):
pipeline เดิมเต็มไปด้วยการ override เฉพาะตำบล/เฉพาะเส้นทางที่เขียนขึ้นระหว่างการตรวจสอบร่วมกับ Ton ทีละจุด เช่น
`SCREEN_DIR_OVERRIDE = {"คลองบางกระทุ่ม": "S"}`, `force_attach("คลองยาง", MAIN, "distributary")`,
`NAME_FIX`/`WS_NAME_FIX`/`WS_NAME_EXCLUDE` ที่เจาะจงชื่อเฉพาะที่พบปัญหาในตำบลนำร่องนี้เท่านั้น — เหมาะสำหรับการ
พิสูจน์แนวคิด (proof of concept) ในตำบลเดียว แต่ไม่ generalize ไปตำบลอื่นโดยอัตโนมัติ หากจะสร้างระบบที่ใช้ได้
จริงกับหลายตำบลผ่านเว็บ จำเป็นต้องมีเอนจินที่คำนวณอัตโนมัติกว้างกว่านี้ (ตามแนวทางเดิมที่ระบุใน `Architecture.
html`: DEM-based flow direction, generic topology snapping) แล้วปล่อยให้ "การแก้ไขเฉพาะจุดที่ข้อมูลอัตโนมัติ
พลาด" เป็นหน้าที่ของชุมชนผ่าน Interactive Canvas (Cytoscape.js) แทนที่จะเป็นโค้ด hardcode ที่วิศวกรเขียนเจาะจง
แต่ละกรณีเอง

## การตัดสินใจ (Decision)

เขียนเอนจินคำนวณ topology + schematic layout **ใหม่ทั้งหมด** สำหรับเว็บแพลตฟอร์ม โดย**ไม่นำโค้ดจาก
`ARCHIVE_pilot_v1_offline_pipeline/04_scripts/*.py` (13-26) มาต่อยอดหรือ port ตรง ๆ** — ใช้เป็นเพียง "บทเรียน/
ข้อสังเกต" อ้างอิงเท่านั้น เอนจินใหม่นี้ยังคงเป็นส่วนออฟไลน์ตาม ADR-001 (รันแบบ batch ไม่ใช่ live server) เขียน
ด้วย Python ต่อไป (ตามสเปคเดิมใน `Architecture.html`: GeoPandas + Shapely + PySheds + NetworkX) แต่ออกแบบให้
**ทั่วไป (generic)** มากกว่าเดิม ลด hardcode เฉพาะชื่อ/ตำบลให้เหลือน้อยที่สุด

## สิ่งที่ยังคงไว้ได้ (ไม่ใช่ "วิธีการ 19 รอบที่ต้องทิ้ง")

- **ข้อมูลดิบ**: shapefile GIS ระดับจังหวัด (mitrearth), DEM30m, ขอบเขตตำบล, KML/CSV ภาคสนามที่ Ton เก็บมา —
  เป็นข้อมูล ไม่ใช่ "วิธี" จึงยังใช้ต่อได้ (อยู่ใน `ARCHIVE_pilot_v1_offline_pipeline/01_data_raw/`, `DEM/`,
  `38 GIS พิษณุโลก (PHITSANULOK)/`)
- **ผังน้ำอ้างอิงจริง** (`ARCHIVE_pilot_v1_offline_pipeline/02_reference_groundtruth/nakhon_pa_mak/ผังน้ำ
  All .png`) — ยังใช้เป็นเกณฑ์ตรวจสอบความถูกต้องของเอนจินใหม่ได้ (ตำบลเดียวกัน มี ground truth ให้เทียบอยู่แล้ว)
- **มาตรฐานสัญลักษณ์ทางการ** (`ARCHIVE_pilot_v1_offline_pipeline/05_schema/waterchart_symbol_schema.md`) — เป็น
  คู่มือมาตรฐานของ สสน. ไม่ใช่วิธีการที่เราคิดเอง ยังต้องยึดตามนี้ต่อไปไม่ว่าเอนจินจะเปลี่ยนแบบไหน
- **บทเรียน/ข้อควรระวังเกี่ยวกับลักษณะข้อมูล** ที่เจอระหว่างทาง 19 รอบ — เช่น บั๊ก field-width truncation
  (ชื่อถูกตัดพอดี 11 ตัวอักษร), artifact ประเภท polygon ความกว้างคลองปนกับแหล่งน้ำจริง, ความหมายจริงของตัวเลขใน
  กล่องคือความจุ (ลบ.ม.) ไม่ใช่พื้นที่ — เป็นความรู้เกี่ยวกับ "ลักษณะข้อมูล" ไม่ใช่โค้ด นำมาใช้ตอนออกแบบเอนจินใหม่
  ให้ตรวจจับปัญหาเหล่านี้แบบทั่วไปได้ (เช่น ตรวจ field-width ของ source data อัตโนมัติ แทนการลิสต์ชื่อที่รู้ว่า
  พังไว้ล่วงหน้า)

## สิ่งที่จะไม่นำมาใช้

- โค้ด `04_scripts/13_extract_topology.py` ถึง `26_build_final_watermap.py` ทั้งหมด (topology_table, dendrogram
  layout algorithm, matplotlib static rendering, ทุก hardcode/override เฉพาะตำบลนี้)
- ผลลัพธ์ตัวเลข/โครงสร้างที่ยืนยันไว้เฉพาะทางในโค้ดเดิม (เช่น "คลองยางเป็น distributary ของ MAIN" ที่
  `force_attach` ไว้ตรง ๆ) จะไม่ถูก assume ไว้ล่วงหน้าในเอนจินใหม่ — เอนจินใหม่ต้องคำนวณ/ตรวจจับด้วยตัวเองใหม่
  ทั้งหมด แล้วให้ Ton/ชุมชนตรวจสอบผ่าน canvas อีกครั้ง (Phase 4: Human-in-the-Loop Verification)

## ผลที่ตามมา (Consequences)

- งานเริ่มต้นใหม่มากกว่าที่ ADR-001 เคยประเมินไว้ (เดิมประเมินว่า "โค้ดใหม่ที่ต้องเขียนน้อย" เพราะจะ reuse เอนจิน
  เดิม — ข้อสมมติฐานนี้เปลี่ยนไปแล้วตาม ADR ฉบับนี้)
- เอนจินใหม่มีโอกาส generalize ไปตำบลอื่นได้ดีกว่า เพราะไม่ผูกกับชื่อ/โครงสร้างเฉพาะของตำบลนำร่อง
- ยังตรวจสอบคุณภาพเอนจินใหม่ได้ด้วยการเทียบผลลัพธ์กับ ground-truth ของตำบลนำร่องเดิม (นครป่าหมาก) และเทียบกับ
  ข้อสรุป/บั๊กที่เคยพบมาแล้ว เป็น regression check ทางอ้อม แม้จะไม่ใช้โค้ดเดิมเลยก็ตาม
- ผลงาน 19 รอบเดิม (สคริปต์, ผังน้ำ static, เอกสาร validation_notes) ยังคงมีค่าเป็นเอกสาร proof-of-concept และ
  แหล่งความรู้เกี่ยวกับลักษณะข้อมูล — เก็บรักษาไว้ครบใน `ARCHIVE_pilot_v1_offline_pipeline/` ตามที่ Ton สั่ง ไม่มี
  การลบ/แก้ไขเนื้อหาใด ๆ เพียงแต่ไม่ใช่ codebase ตั้งต้นของเว็บแพลตฟอร์มใหม่นี้

## รายการงานถัดไป (Action Items)

1. [ ] ยืนยันขอบเขตของเอนจินใหม่กับ Ton: จะ implement ตามสเปคเดิมครบทุกองค์ประกอบทันที (DEM flow-direction ด้วย
   PySheds, topology snapping ด้วย GeoPandas/Shapely, graph ด้วย NetworkX, orthogonal schematic layout
   อัตโนมัติ) หรือเริ่มจากบางส่วนก่อน (เช่น เริ่มจาก topology snapping + graph construction ก่อน ค่อยเพิ่ม DEM
   flow-direction ทีหลัง)
2. [ ] ออกแบบ schema ข้อมูลกราฟ (nodes/edges) ที่เอนจินใหม่จะผลิตออกมา ให้ตรงกับที่ Supabase/frontend Cytoscape
   ต้องการ — เป็น schema ใหม่ ไม่ต้องยึดตาม topology_table เดิม
3. [ ] เลือกตำบลทดสอบชุดแรกของเอนจินใหม่ (แนะนำนครป่าหมากต่อ เพราะมี ground truth ให้เทียบอยู่แล้ว) และสร้าง
   โครงสร้างโฟลเดอร์สำหรับเอนจินใหม่ภายใน `06_web_platform/` (แยกจาก `ARCHIVE_pilot_v1_offline_pipeline/`
   โดยสมบูรณ์)
4. [ ] เริ่มพัฒนาเอนจินใหม่ทีละส่วนตาม Phase 1-3 ของ `00_design_docs/Architecture.html` (Data Ingestion →
   Spatial Integration & Routing → Schematic Transformation) แล้วตรวจผลกับ ground truth ก่อนต่อเข้ากับ
   Supabase/frontend
